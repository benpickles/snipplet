<% content_for :title do %>Edit `<%=h @wave.command %>`<% end %>

<h2>Edit Wave</h2>

<%= render :partial => 'form' %>

<%= button_to 'Delete', @wave, { :confirm => 'Are you sure?', :method => :delete } %>
